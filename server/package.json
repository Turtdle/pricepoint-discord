{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=../.env index.js",
    "start": "node --env-file-if-exists=../.env index.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
